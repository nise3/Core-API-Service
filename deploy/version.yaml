variables:

  # app version, changing version will auto deploy the app to current version
  devBuildImageVersion: "2.3.5"

  # always add prefix k- before production image version
  stagingBuildImageVersion: "2.3.5"

  #app build image
  imageRepository: "registry.nise.gov.bd/nise3-core-api-docker"
